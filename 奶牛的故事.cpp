//#define _CRT_SECURE_NO_WARNINGS
//#include <stdio.h>  
//
//int main() {
//    int n;
//    // 循环读取输入的年份，直到输入为0  
//    while (scanf("%d", &n) && n != 0) {
//        // 特殊处理前三年的情况  
//        if (n == 1 || n == 2) {
//            // 第一年和第二年都只有一头成年奶牛（还没有生育能力）  
//            printf("1\n");
//        }
//        else if (n == 3) {
//            // 第三年还是只有一头成年奶牛（即将在下一年开始生育）  
//            // 但按照题目的简化解释，有时会说第三年有“两头”（包括一头即将生育的奶牛）  
//            // 不过，严格按照年初的计数，第三年年初只有一头奶牛  
//            printf("1\n"); // 这里我们按照严格的年初计数来输出  
//        }
//        else 
//        {
//            // 使用三个变量来跟踪前三年（或说“前两年加上今年”）的奶牛数量  
//            // 注意：这里的变量名是为了解释方便，实际上它们代表的是“多少年前”的奶牛数量  
//            // 而不是“具体的年份”  
//            int cows_two_years_ago = 1; // 两年前的奶牛数量（只有一头初始奶牛）  
//            int cows_one_year_ago = 1;  // 一年前的奶牛数量（还是那头初始奶牛）  
//            int cows_this_year = 1;     // 今年的奶牛数量（初始时，我们只考虑那头初始奶牛）  
//            // 但是，从第四年开始，我们需要加上前两年就存在的奶牛所生出的小奶牛  
//            // 由于第三年年初的奶牛（即这头初始奶牛）在第四年年初会生出小奶牛  
//            // 所以，在n=4时，我们需要将cows_this_year设置为2（1头原有的+1头新生的）  
//            // 不过，由于我们的循环是从4开始的，所以我们可以直接在循环内部处理这个逻辑  
//
//            // 从第四年开始计算，直到第n年  
//            for (int i = 4; i <= n; i++) {
//                // 计算下一年的奶牛数量  
//                // 下一年的奶牛数量等于今年的奶牛数量加上两年前就存在的奶牛数量所生出的小奶牛  
//                // （注意：这里的“两年前”是指在当前年份之前的两年，而不是从初始年份算起的两年）  
//                int cows_next_year = cows_this_year + cows_two_years_ago;
//
//                // 更新变量以用于下一次迭代  
//                cows_two_years_ago = cows_one_year_ago; // 两年前的奶牛数量变成了一年前的  
//                cows_one_year_ago = cows_this_year;    // 一年前的奶牛数量变成了今年的  
//                cows_this_year = cows_next_year;       // 今年的奶牛数量变成了计算出的下一年的奶牛数量  
//            }
//
//            // 打印第n年的奶牛数量  
//            printf("%d\n", cows_this_year);   
//        }
//    }
//    return 0;
//}
//
//
//
//
